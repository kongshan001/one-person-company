# Playbook: 内存使用率过高

**级别**: 🟡 Warning  
**触发条件**: 内存 > 90%

## 自动处理

1. 检查内存占用：`free -m`
2. 识别高内存进程：`ps aux --sort=-%mem | head -10`
3. 清理缓存（如安全）：`sync && echo 3 > /proc/sys/vm/drop_caches`
4. 重启内存泄漏的服务

## 人工介入

1. 分析内存泄漏原因
2. 优化代码或增加 swap

## 通知

- 清理缓存后通知
- 内存持续 > 95% → 发送紧急告警
